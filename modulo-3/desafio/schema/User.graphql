# @format

type User {
	id: ID!
	name: String!
	email: String!
	profiles: [Profile]
}

input UserInput {
	name: String!
	email: String!
	password: String!
	profiles: [ProfileFiltre]
}

input UserFiltre {
	id: Int
	email: String
}
