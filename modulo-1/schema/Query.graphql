# @format
# Pontos de entrada da API
type Query {
	hello: String
	hourNow: Date
	# usando o Type personalizado User
	userLogged: User
	productSpotlight: Product
	# criando Array
	# obrigatoriamente retorna um array com elementos
	numbers: [Int!]!
	usersList: [User]!
	user(id: ID): User
	profiles: [Profile]
	profile(id: Int): Profile
}
